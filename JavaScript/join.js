// join()
const fruits = ['apple', 'banana', 'cherry'];

// join() une todos los elementos del array en una cadena, entre parentesis indico que quiero que haya un espacio entre cada palabra
const fruitString = fruits.join(' ');

// Imprime la cadena resultante con los elementos del array unidos por espacios entre cada palabra
console.log(fruitString);