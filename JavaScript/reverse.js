// reverse()
const numbers = [1, 2, 3, 4, 5];

// reverse() invierte el orden de los elementos del array en su lugar
numbers.reverse();

// Imprime el array con el orden de los elementos invertido
console.log(numbers);