// toString()
const numbers = [1, 2, 3, 4, 5];

// toString() convierte el array en una cadena de texto, separada por comas
const numbersString = numbers.toString();

// Imprime el array convertido en cadena
console.log(numbersString);