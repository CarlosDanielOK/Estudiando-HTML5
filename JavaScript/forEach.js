// forEach() ejecuta una función para cada elemento de un array. NO DEVUELVE UN NUEVO ARRAY, simplemente itera sobre los elementos. Su uso común es realizar acciones en cada elemento de un array, como imprimir en consola.

const numbers = [1, 2, 3];
numbers.forEach( (num) => console.log(num) ); // 1 2 3
